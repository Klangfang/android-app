<?xml version="1.0" encoding="utf-8"?>
<!-- TODO Should convert this into a constraint layout, to get rid of the spacing layout -->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="10dp"
    android:baselineAligned="false"
    >

    <com.wfm.soundcollaborations.editor.views.composition.CompositionScrollView
        android:id="@+id/hsv_holder"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:overScrollMode="never"
        >
        <!-- The following layout is used to create space between trackwatches,
        tracks and right screen margin -->
        <LinearLayout
            android:id="@+id/ll_space"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:background="@color/black"
            >
            <!-- The following layout holds the tracks -->
            <LinearLayout
                android:id="@+id/tvs"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingTop="4dp"
                android:paddingBottom="4dp"
                android:background="@color/black"
                />

        </LinearLayout>
    </com.wfm.soundcollaborations.editor.views.composition.CompositionScrollView>
    <!-- The following layout holds the trackwatches -->
    <LinearLayout
        android:id="@+id/twvs"
        android:layout_width="50dp"
        android:layout_height="match_parent"
        android:orientation="vertical" />
</FrameLayout >